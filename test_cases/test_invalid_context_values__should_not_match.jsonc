{
  // Given: A segment with IS_SET condition against interesting context values:
  // - `$.identity`: an object, not a primitive value — should not be treated as a context value, expected to be ignored
  // - `trait key "with quotes"`: a smoke test for escaped quotes
  // - `$.leads.to.nowhere`: a non-existent path
  // When: An evaluation context
  // Then: The context should not be considered part of the segment
  "$schema": "https://raw.githubusercontent.com/Flagsmith/engine-test-data/refs/tags/v2.0.0/schema.json",
  "context": {
    "environment": {
      "key": "key",
      "name": "Environment"
    },
    "segments": {
      "99": {
        "key": "99",
        "name": "segment_invalid_context_values",
        "rules": [
          {
            "type": "ALL",
            "conditions": [
              {
                "operator": "IS_SET",
                "property": "$.identity", // Objects should not be treated as context values
                "value": ""
              },
              {
                "operator": "IS_SET",
                "property": "trait key \"with quotes\"",
                "value": ""
              },
              {
                "operator": "IS_SET",
                "property": "$.leads.to.nowhere",
                "value": ""
              }
            ]
          }
        ]
      }
    }
  },
  "result": {
    "flags": {},
    "segments": []
  }
}