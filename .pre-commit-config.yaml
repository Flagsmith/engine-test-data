repos:
  - repo: local
    hooks:
      - id: prettier
        name: prettier
        language: node
        entry: prettier
        additional_dependencies:
          - prettier@3.8.3
        args:
          - --check
        files: \.jsonc?$
